workspace:
  base: /go
  path: spriderGo

pipeline:
  build:
    image: golang:latest
    commands:
      - go env -w GOPROXY=https://goproxy.cn
      - go mod download github.com/gocolly/colly/v2
      - go run rate_limit.go
      